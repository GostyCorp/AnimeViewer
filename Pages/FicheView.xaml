<UserControl
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
	x:Class="AnimeViewer.Pages.FicheView"
	mc:Ignorable="d"
	d:DesignHeight="200"
	d:DesignWidth="200">
	<Grid Width="200" Height="200">
		<Button Click="Button_Click" Padding="0,0,0,0">
			<StackPanel Orientation="Vertical" Height="200" Width="200" Background="#00000000">
				<Image Source="{Binding UrlImage}" Width="180" Height="130" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10,10,10,10" />
				<TextBlock Text="{Binding Title}" FontFamily="Century Gothic" FontSize="12" TextAlignment="Center" TextWrapping="Wrap" Width="200" Height="50" Padding="10, 0, 10, 10" Foreground="#FFFDFDFD"/>
				<ui:NumberBox x:Name="IdNumber" Text="{Binding Id}" TextWrapping="Wrap" Min="0" Max="100000" IntegersOnly="True" Visibility="Hidden" />
			</StackPanel>
		</Button>
	</Grid>
</UserControl>