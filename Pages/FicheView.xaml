<UserControl
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
	x:Class="AnimeViewer.Pages.FicheView"
	mc:Ignorable="d"
	d:DesignHeight="200"
	d:DesignWidth="200">
	<Grid>
		<Button Click="Button_Click">
			<StackPanel Orientation="Vertical" Background="#FF464646" Height="200" Width="200" HorizontalAlignment="Center" VerticalAlignment="Center">
				<Image Source="{Binding UrlImage}" Width="200" Height="150" />
				<Label VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Height="50">
					<StackPanel>
						<Label Content="{Binding Title}" />
						<Label Content="{Binding Number}" />
					</StackPanel>
				</Label>
				<ui:NumberBox x:Name="IdNumber" Text="{Binding Id}" TextWrapping="Wrap" Min="0" Max="100000" IntegersOnly="True" Visibility="Hidden" />
			</StackPanel>
		</Button>
	</Grid>
</UserControl>